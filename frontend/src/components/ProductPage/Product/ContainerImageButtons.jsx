import React from 'react'

const ContainerImageButtons = ({children}) => {
  return (
    <div className='flex justify-center space-x-2'>
      {children}
    </div>
  )
}

export default ContainerImageButtons
